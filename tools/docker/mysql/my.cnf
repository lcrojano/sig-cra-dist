[mysqld]
# Limit InnoDB buffer pool to ~50% of the container RAM
innodb_buffer_pool_size = 512M

# Reduce connections if not needed
max_connections = 50

# Optional: reduce thread concurrency
innodb_thread_concurrency = 4

 