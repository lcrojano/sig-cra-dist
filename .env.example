# ==============================================================================
# SIG Platform Production Environment Configuration
# Copy this file to .env and fill in your values
# ==============================================================================

# Domain Configuration (REQUIRED)
# Your production domain without https:// prefix
DOMAIN=localhost

# Database Configuration (REQUIRED)
DB_DATABASE=sig_platform
DB_USERNAME=sig_user
DB_PASSWORD=pasword
DB_ROOT_PASSWORD=pasword

# Mail Configuration (OPTIONAL - for notifications and system emails)
MAIL_FROM_NAME="Corporación Autónoma Regional del Atlántico"
MAIL_FROM_ADDRESS=sig-cra@metamagagency.com
MAIL_MAILER=sendgrid

# SendGrid Configuration
SENDGRID_API_KEY=


 
# Optional: Email for Let's Encrypt SSL notifications
# LETSENCRYPT_EMAIL=admin@your-domain.com
 

# ==============================================================================
# IMPORTANT SECURITY NOTES:
# ==============================================================================
# 1. Replace 'your-domain.com' with your actual domain
# 2. Use strong, unique passwords for database credentials
# 3. Never use the example SendGrid API key - get your own from SendGrid
# 4. Ensure your domain DNS points to this server before deployment
# 5. The deploy script will handle SSL certificate generation automatically
# 6. Keep this file secure and never commit it to version control
# 7. Update MAIL_FROM_ADDRESS to use your actual domain for better deliverability
# ==============================================================================